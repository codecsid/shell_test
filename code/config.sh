# For running locally replace DEFAULT with your team name and password respectively.

[ "$TEAM" == "" ] && team="grb" || team="$TEAM"
[ "$PASSWORD" == "" ] && password="grb" || password="$PASSWORD"

baseApi="localhost:3000"
